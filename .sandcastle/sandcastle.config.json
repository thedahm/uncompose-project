{
  "repoChecks": "`cargo test` at the repo root runs everything, plus `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` (see CONTRIBUTING.md). If the touched code has other documented checks (e.g. Python packaging via `ci/smoke-wheel.sh`), run those too.",
  "copyToWorktree": [
    "node_modules",
    "target"
  ],
  "roster": {
    "haiku": {
      "provider": "claudeCode",
      "model": "claude-haiku-4-5"
    },
    "sonnet": {
      "provider": "claudeCode",
      "model": "claude-sonnet-5"
    },
    "opus": {
      "provider": "claudeCode",
      "model": "claude-opus-5"
    },
    "fable": {
      "provider": "claudeCode",
      "model": "claude-fable-5"
    }
  },
  "implementerDefaults": {
    "tier": "opus",
    "effort": "high"
  }
}
