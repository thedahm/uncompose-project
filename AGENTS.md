# Uncompose Project

Local-first provenance for derived audio.

This is the canonical agent-instructions file for `uncompose-project`, per Uncompose
family convention. `CLAUDE.md` points here.

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `thedahm/uncompose-project` (via `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
